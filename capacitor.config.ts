import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.humedrexito.app',
  appName: 'Humedrexito (Off Queroga)',
  webDir: 'dist/myapp/browser'
};

export default config;
